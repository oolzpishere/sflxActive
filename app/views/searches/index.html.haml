%h3 Search result

- @searches.records.each do |search|
  /-byebug
  %p= link_to search.title, generate_path(search)
  = sanitize search.body.truncate(60, separator: ' ', omission: '... (more)') if search.body.present?
  /= s.highlight.body


